<style>
  .mask {
    mask-image: linear-gradient(black 90%, transparent);
  }
</style>

<section
  class="mask relative flex flex-col md:flex-row items-center justify-center min-h-[90vh] w-full bg-gradient-to-br from-white via-blue-50 to-blue-200 px-6 md:px-20 text-center md:text-left"
>
  <div class="max-w-xl md:mr-12 mb-12 md:mb-0">
    <h1 class="text-4xl md:text-6xl font-extrabold leading-tight text-gray-900">
      Explora Nuestro<br />
      <span class="text-blue-600">Catálogo de Zapatos</span>
    </h1>
    <p class="text-lg text-gray-600 mt-6">
      Descubre la mejor selección de calzado para cada ocasión. Calidad y estilo
      en un solo lugar. Encuentra tus marcas favoritas y luce siempre a la moda.
    </p>
    <div class="mt-8 flex justify-center">
      <a
        href="#catalogue"
        class="cursor-pointer px-12 py-4 bg-blue-500 text-white text-2xl rounded-full hover:bg-blue-700 hover:-translate-y-1 transition font-medium shadow-lg"
      >
        Explorar Ahora
      </a>
    </div>
  </div>

  <div class="relative overflow-visible">
    <img
      src="https://ciconceptstore.com/cdn/shop/products/5_19.png?v=1676123644&width=800"
      alt="Zapato destacado"
      class="w-[400px] md:w-[750px] animate-float drop-shadow-2xl shadow-black/30"
    />
  </div>
</section>

<!-- Animación Flotante -->
<style>
  @keyframes float {
    0% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-15px);
    }
    100% {
      transform: translateY(0px);
    }
  }

  .animate-float {
    animation: float 4s ease-in-out infinite;
  }
</style>
